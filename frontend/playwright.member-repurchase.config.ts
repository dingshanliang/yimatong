import { defineConfig, devices } from "@playwright/test";

/**
 * Evidence-only config. The four-layer runner treats the real backend/Admin/H5
 * processes as explicit preconditions and never starts a second hidden stack.
 */
export default defineConfig({
  testDir: "./e2e",
  fullyParallel: false,
  workers: 1,
  retries: 0,
  timeout: 120_000,
  reporter: [["list"]],
  use: {
    baseURL: process.env.EVIDENCE_ADMIN_BASE_URL || "http://localhost:3000",
    trace: "retain-on-failure",
    screenshot: "only-on-failure",
  },
  projects: [
    {
      name: "chromium",
      use: {
        ...devices["Desktop Chrome"],
        viewport: { width: 1280, height: 720 },
      },
    },
  ],
});
